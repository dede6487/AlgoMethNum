function [K_h] = Stiff1(N)

n = (N-1)^2;
h = 1/N;

i = [1:n,1:n-1,2:n];
j = [1:n,2:n,1:n-1];

b = -ones(n-1,1);

b1 = b;

%for i = 1:N-2:(N-2)^2
%    b1(i+1) = 0;
%    i = i+1;
%end

%b1(1:N-2:n-N-1) = zeros(N-2,1);

s = [4*ones(n,1)',b1',b1'];

K_h = sparse(i,j,s,n,n);

end